Create a new FastAPI endpoint following these rules:

- Use async def.
- Define request and response Pydantic models.
- Validate all incoming data.
- Use dependency injection for DB/session objects.
- Include example response in the docstring.
- Handle errors with custom HTTPExceptions.
- Add minimal tests using pytest and pytest-asyncio.
